[phases.setup]
nixPkgs = ["python310", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "cd 'Reddit Ovarra' && pip install -r requirements.txt"
]

[start]
cmd = "cd 'Reddit Ovarra' && python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
