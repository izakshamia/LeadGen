[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "cd 'Reddit Ovarra' && python3 -m pip install -r requirements.txt"
]

[start]
cmd = "cd 'Reddit Ovarra' && python3 -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
